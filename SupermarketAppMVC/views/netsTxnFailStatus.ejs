<%- include('partials/header') %>

<div class="container mt-5">
  <div class="row justify-content-center">
    <div class="col-md-8 text-center">
      <img src="/images/redCross.png" alt="Transaction Failed" class="img-fluid mb-3" style="max-width:180px;" />
      <h2 class="text-danger mb-2">TRANSACTION FAILED</h2>
      <p class="lead"><%= message || 'Your payment could not be completed.' %></p>
      <% if (typeof responseCode !== 'undefined' && responseCode) { %>
        <p class="text-muted mb-1">Response code: <%= responseCode %></p>
      <% } %>
      <% if (typeof instructions !== 'undefined' && instructions) { %>
        <p class="text-muted"><%= instructions %></p>
      <% } else { %>
        <p class="text-muted">Please try again or choose another payment method.</p>
      <% } %>

      <div class="d-flex justify-content-center mt-4">
        <a href="/checkout" class="btn btn-primary me-2">Back to Checkout</a>
        <a href="/" class="btn btn-outline-secondary">Continue Shopping</a>
      </div>
    </div>
  </div>
</div>

<%- include('partials/footer') %>
